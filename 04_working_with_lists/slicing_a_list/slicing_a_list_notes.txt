=====================================================================================================================================================
                                                            SLICING A LIST
=====================================================================================================================================================

OVERVIEW
-----------------------------------------------------------------------------------------------------------------------------------------------------
In Python, working with a specific group of items in a list is called slicing. Slices allow developers to extract, iterate through, or copy 
subsets of data efficiently without modifying the original collection.


1. SLICING A LIST
-----------------------------------------------------------------------------------------------------------------------------------------------------
* Basic Syntax: list_name[start_index:stop_index]. Python stops one item before the stop_index (off-by-one mechanism).

Code Example:
>>> players = ['charles', 'martina', 'michael', 'florence', 'eli']
>>> print(players[0:3])

Output:
['charles', 'martina', 'michael']

* Starting from Beginning: Omitting the first index defaults to index 0.

Code Example:
>>> print(players[:4])

Output:
['charles', 'martina', 'michael', 'florence']

* Slicing to the End: Omitting the second index slices through the end of the list.

Code Example:
>>> print(players[2:])

Output:
['michael', 'florence', 'eli']

* Negative Indices: Use negative values to slice relative to the end of the list.

Code Example:
>>> print(players[-3:])

Output:
['michael', 'florence', 'eli']


2. LOOPING THROUGH A SLICE
-----------------------------------------------------------------------------------------------------------------------------------------------------
* Subsets in Loops: You can use a slice directly in a for loop to iterate over a subset instead of the entire list.

Code Example:
>>> players = ['charles', 'martina', 'michael', 'florence', 'eli']
>>> print("Here are the first three players on my team:")
>>> for player in players[:3]:
>>>     print(player.title())

Output:
Here are the first three players on my team:
Charles
Martina
Michael


3. COPYING A LIST
-----------------------------------------------------------------------------------------------------------------------------------------------------
* Correct Copying Syntax: Use an entire list slice ([:]) to make a true independent copy of a list.

Code Example:
>>> my_foods = ['pizza', 'falafel', 'carrot cake']
>>> friend_foods = my_foods[:]
>>> my_foods.append('cannoli')
>>> friend_foods.append('ice cream')
>>> print(my_foods)
>>> print(friend_foods)

Output:
['pizza', 'falafel', 'carrot cake', 'cannoli']
['pizza', 'falafel', 'carrot cake', 'ice cream']

* Reference Warning: Setting friend_foods = my_foods makes both variables point to the exact same list in memory (modifying one alters both).

Code Example:
>>> friend_foods = my_foods
>>> my_foods.append('cannoli')
>>> friend_foods.append('ice cream')
>>> print(my_foods)
>>> print(friend_foods)

Output:
['pizza', 'falafel', 'carrot cake', 'cannoli', 'ice cream']
['pizza', 'falafel', 'carrot cake', 'cannoli', 'ice cream']


4. TRY IT YOURSELF EXERCISES
-----------------------------------------------------------------------------------------------------------------------------------------------------
File naming standard: Use descriptive, lowercase snake_case names (e.g., slices.py).

- 4-10. Slices:
  Using one of the programs you wrote in this chapter, add several lines to the end of the program to print the first three items, three middle 
  items, and last three items using slices.

- 4-11. My Pizzas, Your Pizzas:
  Make a copy of the list of pizzas using [:]. Add a new pizza to the original list and a different pizza to friend_pizzas. Use for loops to print 
  both lists and prove they are separate.

- 4-12. More Loops:
  Choose a version of foods.py and write two for loops to print each list of foods.
