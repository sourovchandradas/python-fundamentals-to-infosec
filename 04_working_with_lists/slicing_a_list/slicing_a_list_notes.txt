=====================================================================================================================================================
                                                            SLICING A LIST
=====================================================================================================================================================

OVERVIEW
-----------------------------------------------------------------------------------------------------------------------------------------------------
In Python, working with a specific group of items in a list is called slicing. Slices allow developers to extract, iterate through, or copy 
subsets of data efficiently without modifying the original collection.


1. SLICING A LIST
-----------------------------------------------------------------------------------------------------------------------------------------------------
* Basic Syntax: list_name[start_index:stop_index]. Python stops one item before the stop_index (off-by-one mechanism).

Code Example (First Three Items):
>>> players = ['charles', 'martina', 'michael', 'florence', 'eli']
>>> print(players[0:3])

Output:
['charles', 'martina', 'michael']

* Middle Elements: Starting at index 1 and ending before index 4 returns elements at indices 1, 2, and 3.

Code Example:
>>> players = ['charles', 'martina', 'michael', 'florence', 'eli']
>>> print(players[1:4])

Output:
['martina', 'michael', 'florence']

* Omitting Start Index: Omitting the first index defaults to index 0 ([:4]).

Code Example:
>>> players = ['charles', 'martina', 'michael', 'florence', 'eli']
>>> print(players[:4])

Output:
['charles', 'martina', 'michael', 'florence']

* Omitting Stop Index: Omitting the second index slices from the starting point through the end of the list ([2:]).

Code Example:
>>> players = ['charles', 'martina', 'michael', 'florence', 'eli']
>>> print(players[2:])

Output:
['michael', 'florence', 'eli']

* Negative Indices: Use negative values to slice relative to the end of the list ([-3:] gets the last 3 items).

Code Example:
>>> players = ['charles', 'martina', 'michael', 'florence', 'eli']
>>> print(players[-3:])

Output:
['michael', 'florence', 'eli']


2. LOOPING THROUGH A SLICE
-----------------------------------------------------------------------------------------------------------------------------------------------------
* Subsets in Loops: You can use a slice directly in a for loop statement to iterate over a subset instead of the entire list.

Code Example:
>>> players = ['charles', 'martina', 'michael', 'florence', 'eli']
>>> print("Here are the first three players on my team:")
>>> for player in players[:3]:
>>>     print(player.title())

Output:
Here are the first three players on my team:
Charles
Martina
Michael


3. COPYING A LIST
-----------------------------------------------------------------------------------------------------------------------------------------------------
* Correct Copying Syntax: Use an entire list slice ([:]) to make a true independent copy of a list in memory.

Step 1: Creating a copy of the original list using slice [:].
Code Example:
>>> my_foods = ['pizza', 'falafel', 'carrot cake']
>>> friend_foods = my_foods[:]
>>> print("My favorite foods are:")
>>> print(my_foods)
>>> print("\nMy friend's favorite foods are:")
>>> print(friend_foods)

Output:
My favorite foods are:
['pizza', 'falafel', 'carrot cake']

My friend's favorite foods are:
['pizza', 'falafel', 'carrot cake']

Step 2: Proving the two lists are independent by appending a unique item to each list.
Code Example:
>>> my_foods = ['pizza', 'falafel', 'carrot cake']
>>> friend_foods = my_foods[:]
>>> my_foods.append('cannoli')
>>> friend_foods.append('ice cream')
>>> print("My favorite foods are:")
>>> print(my_foods)
>>> print("\nMy friend's favorite foods are:")
>>> print(friend_foods)

Output:
My favorite foods are:
['pizza', 'falafel', 'carrot cake', 'cannoli']

My friend's favorite foods are:
['pizza', 'falafel', 'carrot cake', 'ice cream']

Note : The output shows that 'cannoli' now appears in our list of favorite foods but 'ice cream' doesn’t. We can see that 'ice cream' now appears
       in our friend’s list but 'cannoli' doesn’t.

* Reference Warning (Copying Without a Slice): Setting friend_foods = my_foods links both variables to the EXACT SAME list in memory.

Code Example (Incorrect Approach):
>>> my_foods = ['pizza', 'falafel', 'carrot cake']
>>> friend_foods = my_foods
>>> my_foods.append('cannoli')
>>> friend_foods.append('ice cream')
>>> print("My favorite foods are:")
>>> print(my_foods)
>>> print("\nMy friend's favorite foods are:")
>>> print(friend_foods)

Output:
My favorite foods are:
['pizza', 'falafel', 'carrot cake', 'cannoli', 'ice cream']

My friend's favorite foods are:
['pizza', 'falafel', 'carrot cake', 'cannoli', 'ice cream']

Note : Instead of storing a copy of my_foods in friend_foods, we set friend_foods equal to my_foods. This syntax actually tells Python to connect
       the new variable friend_foods to the list that is already contained in my_foods, so now both variables point to the same list. As a
       result, when we add 'cannoli' to my_foods, it will also appear in friend_foods. Likewise 'ice cream' will appear in both lists, even
       though it appears to be added only to friend_foods.


4. TRY IT YOURSELF EXERCISES
-----------------------------------------------------------------------------------------------------------------------------------------------------
File naming standard: Use descriptive, lowercase snake_case names (e.g., slices.py).

- 4-10. Slices:
  Using one of the programs from this chapter, add code at the end to print:
  1. "The first three items in the list are:" using [:3]
  2. "Three items from the middle of the list are:" using [1:4]
  3. "The last three items in the list are:" using [-3:]

- 4-11. My Pizzas, Your Pizzas:
  Start with Exercise 4-1. Copy the pizza list using friend_pizzas = my_pizzas[:]. Add a new pizza to original list and a different pizza to 
  friend_pizzas. Use separate for loops to print both lists and prove they are independent.

- 4-12. More Loops:
  Choose a version of foods.py and write two for loops to print each list of foods cleanly.
